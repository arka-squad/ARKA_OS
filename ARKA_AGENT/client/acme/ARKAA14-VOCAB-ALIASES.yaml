id: ARKAA14-VOCAB-ALIASES
version: 1.0.0
title: "Alias vocabulaire domain → actions / inputs"
exports:
  aliases:
    # intents → action_keys (complète les alias déjà gérés par 12)
    ORDER_CREATE: TICKET_CREATE
    ORDER_CLOSE:  TICKET_CLOSE
    # champs d'input
    orderId:      ticketId
    requestId:    ticketId