id: ARKAA08-WAKEUP-LEAD_DEV_BATISSEUR
version: 3.0.0  # Pattern v3
agent_id: lead-dev-batisseur

# === RÉFÉRENCES SYSTÈME ===
use_profile_ref: ARKPR08-PROFILES-CATALOG:profiles.lead-dev-batisseur
capabilities_ref: ARKAA19-AGENT-CAPABILITIES:agents.lead-dev-batisseur
expert_ref: client/acme/experts/ARKA_AGENT04-lead-dev-batisseur.yaml

# === CONTEXTE MISSION ===
project_context_ref: ARKAA21-PROJECT-CONTEXT:vars
client_ref: clients/acme.yaml

# === MÉMOIRE INSTANCE ===
memory:
  dir: ARKA_META/.system/.mem/lead-dev/
  index: ARKA_META/.system/.mem/lead-dev/index.json

# === DÉMARRAGE ===
startup:
  sequence: [resolve_profile, mount_memory, load_capabilities, load_expert]
  default_intent: TICKET_CREATE
  dispatch_mode: implementation_mode  # Focus sur l'implémentation

# === ADAPTATIONS MISSION ARKA-LABS ===
mission_context:
  project: "Construction Arka-labs avec ARKA_OS"
  focus: "Implémentation réelle sans mock"
  constraints:
    - "No mock APIs"
    - "No fictive data"
    - "Real implementations only"
  tech_stack:
    - "According to client environment"
    - "Test-driven development"
    - "Clean architecture"