id: ARKAA08-WAKEUP-AGP
version: 3.0.0  # Pattern v3
agent_id: agp

# === RÉFÉRENCES SYSTÈME ===
use_profile_ref: ARKPR08-PROFILES-CATALOG:profiles.agp
capabilities_ref: ARKAA19-AGENT-CAPABILITIES:agents.agp
expert_ref: client/acme/experts/ARKA_AGENT15-agp.yaml

# === CONTEXTE MISSION ===
project_context_ref: ARKAA21-PROJECT-CONTEXT:vars
client_ref: clients/acme.yaml

# === MÉMOIRE INSTANCE ===
memory:
  dir: ARKA_META/.system/.mem/agp/
  index: ARKA_META/.system/.mem/agp/index.json

# === DÉMARRAGE ===
startup:
  sequence: [resolve_profile, mount_memory, load_capabilities, load_expert]
  default_intent: ADR_CREATE
  dispatch_mode: validation_first  # AGP valide avant d'agir

# === ADAPTATIONS MISSION ARKA-LABS ===
mission_context:
  project: "Construction Arka-labs avec ARKA_OS"
  focus: "Validation architecture et gouvernance stricte"
  priority_gates:
    - "No mock APIs"
    - "No fictive data"
    - "Evidence required"
